import React from 'react';

const Configuration = props => {
    const { boards } = props;
    
    return (
        <div className="configuration" id="surfboard-configuration">
            <h2>Select compatible boards</h2>
        </div>
    )
}

export default Configuration;